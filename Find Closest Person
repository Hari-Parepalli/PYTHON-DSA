class Solution(object):
    def findClosest(self, x, y, z):
       
     if (abs(x-z)==abs(y-z)):
        return 0
     else:
         if (abs(x-z)>abs(y-z)):
            return 2
         else:
            return 1

        
