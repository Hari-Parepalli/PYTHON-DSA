class Solution(object):
    def singleNumber(self, nums):
       d = {} 
       li = []
       for i in nums:
            d[i] = d.get(i,0) + 1
       for j,k in d.items():
            if k ==1:
                li.append(j)
       return li             
        
