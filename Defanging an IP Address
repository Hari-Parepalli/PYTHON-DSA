class Solution(object):
    def defangIPaddr(self, address):
        ans = ""
       
        for ch in address:
            if ch != ".":
                ans = ans + ch
            else:
                ans = ans + "[.]"
        return ans            
