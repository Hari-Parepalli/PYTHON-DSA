class Solution(object):
    def minimumAverage(self, nums):
     
     ans = float('inf')
     nums.sort()
     l = 0
     r = len(nums) -1
     while l<r:
        avg = (nums[l] + nums[r])/2.0
        ans = min(ans,avg)
        l+=1
        r-=1
     return ans
